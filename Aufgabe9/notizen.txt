//TS//
    if (title == "") {
        alert ("type a task!!!!!!!");
    
    }
    else {

//HTML//
    <ul id="liste">
                <li>
                    <input type="checkbox"> <label>Weihnachtsbaum schmücken</label>
                </li>
                <li>Geschenke verpacken</li>

            </ul>


//Mein Versuch TS//

interface Todo {
    title: string;
    done: boolean;
    }
    
let todos: Todo[] = [
    {
    title: "Geschenke kaufen",
    done: true
    }, 
    {
    title: "Glühwein warm machen",
    done: true
    }
    ];
    
for (let i: number = 0; i < todos.length; i++) {
    document.querySelector(".todoListe ul").innerHTML += "<li>" + todos[i].title + " [ " + todos[i].done + " ] </li>";
    }

function newTask (title: string, done: boolean): void {

        let newTodo: HTMLElement = document.createElement("li");
        newTodo.setAttribute ("class", "newTaskElement");
        document.body.appendChild(newTodo);
        console.log("okay");

        let myInputElement: HTMLInputElement = document.querySelector(".addTask");

        let inputValue: string = myInputElement.value;
        newTodo.innerHTML = inputValue;
        console.log(inputValue);
        
        let checkBox: HTMLElement = document.createElement("input");
        checkBox.setAttribute("type", "checkbox");
        checkBox.setAttribute("class", "done");
        document.body.appendChild(checkBox);
    
    }

document.getElementById("add").addEventListener("click", function(): void {
    newTask (this.title, true);
});

//Mein Versuch Ende//


//Test TS//

interface Todo {
    title: string;
    done: boolean;
    }
    
let todo: Todo[] = [
    {
    title: "Geschenke kaufen",
    done: true
    }, 
    {
    title: "Glühwein warm machen",
    done: true
    }
    ];
    
for (let i: number = 0; i < todos.length; i++) {
    document.querySelector(".todoListe ul").innerHTML += "<li>" + todos[i].title + " [ " + todos[i].done + " ] </li>";
    }

function newTodo (title: string, completed: boolean): void {
    {
       //Neues ListenElement//
       let newTask: HTMLElement = document.createElement("li");
       newTask.setAttribute ("class", "newListElmt");
       document.body.appendChild(newTask);

       //in der Liste soll der Input des Feldes stehen//
       console.log("ok");
       let myInputElement: HTMLInputElement = document.querySelector(".addTask");
       let inputValue: string = myInputElement.value;
       newTask.innerHTML = inputValue;
       console.log(inputValue);
       
       //checkbox soll zu meinem ListenElement hinzugefügt werden/
       let checkBox: HTMLElement = document.createElement("input");
       checkBox.setAttribute("type", "checkbox");
       checkBox.setAttribute("class", "check");
       document.body.appendChild(checkBox);

       //DeleteButton soll hinzugefügt werden/
       let deleteBtn: HTMLElement = document.createElement("i");
       deleteBtn.setAttribute("class", "fas fa-trash");
       document.body.appendChild(deleteBtn);
       }
       
    document.getElementById("add").addEventListener("click", function(): void {
        newTask (this.title, true);
    });

      
   }

// Test TS Ende //


//// Funktionierende Funktion zum hinzufügen von Tasks, allerdings ohne Checkbox oder Müll Icon ////

let input: HTMLInputElement = document.querySelector("#addTask");
let addButton: HTMLInputElement = document.querySelector("#addBtn");
let todoList: HTMLElement = document.querySelector("#todoListe");
let todoElement: HTMLCollectionOf<HTMLLIElement> = document.getElementsByTagName("li"); // HTMLCollectionOf<HTMLLIElement hat der Linter vorgeschlagen. Was genau ist das? //

// Funktion zum Hinzufügen von ToDos//

function addTodo(): void {
    let inputText: string = input.value;

    if (inputText == "") {
        alert("type a task!!!!!");
    }
    else {
        let li: HTMLLIElement = document.createElement("li");
        li.innerHTML = inputText;
        todoList.insertBefore(li, todoList.childNodes[0]);
        input.value = "";
        
    }
}

document.querySelector("#addBtn").addEventListener("click", function(): void {
    addTodo();
});

//// Funktionierende Funktion zum hinzufügen von Tasks, allerdings ohne Checkbox oder Müll Icon ENDE ////


todoList.insertBefore(todoElement, todoList.childNodes[0]);
//oder //
let todo: Text = document.createTextNode(input.value);